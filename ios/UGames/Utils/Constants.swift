import Foundation
import UIKit

enum Constants {
    enum Network {
        static let host = "yandex.ru"
    }

    enum UI {
        static var screenSize: CGSize {
            UIScreen.main.bounds.size
        }
    }
}
